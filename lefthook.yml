commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    # Format all files with Prettier (non-Python)
    prettier:
      glob: "*.{js,ts,jsx,tsx,vue,json,md,yml,yaml}"
      run: npx prettier --write {staged_files}
      stage_fixed: true

    # Python: Processing
    ruff-check-processing:
      glob: "processing/**/*.py"
      run: cd processing && uv run ruff check --fix {staged_files}
      stage_fixed: true
    ruff-format-processing:
      glob: "processing/**/*.py"
      run: cd processing && uv run ruff format {staged_files}
      stage_fixed: true

    # Python: Backend
    ruff-check-backend:
      glob: "backend/**/*.py"
      run: cd backend && uv run ruff check --fix {staged_files}
      stage_fixed: true
    ruff-format-backend:
      glob: "backend/**/*.py"
      run: cd backend && uv run ruff format {staged_files}
      stage_fixed: true

    # Frontend: ESLint
    eslint:
      glob: "frontend/**/*.{js,ts,jsx,tsx,vue}"
      run: cd frontend && npm run lint -- --fix {staged_files}
      stage_fixed: true
